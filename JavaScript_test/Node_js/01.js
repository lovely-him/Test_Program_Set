console.log('holle work!');

// 知识点：

// 1. 安装node、安装vscode、安装创建应用，准备好开放环境等

// 2. 安装npm，用于管理node项目，在命令行输入 npm init 指令可以快速创建node项目的 package.json 配置文件。设置相关参数。

// 3. 在 package.json 配置文件中的 scripts 分类下，可以写入四个脚本内容 ："start"、"test"、"build"、"lint"。相当于在运行时，可以快速输入终端指令。

// 4. 管理 package.json 配置文件中的  "dependencies" 和 "devDependencies" 属性，前者是生产用的包，后者是调试用的包。可通过 npm uninstall 删除包

// 5. package-lock.json 文件的主要性 